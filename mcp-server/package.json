{
  "name": "dns-policy-manager-mcp",
  "version": "1.0.0",
  "description": "MCP server for Windows DNS Policy Manager — exposes DNS server/zone/record queries to AI agents",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^6"
  }
}
