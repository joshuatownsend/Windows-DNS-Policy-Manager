# Bridge runtime
bridge.log
server/credentials/

# Playwright MCP artifacts
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# Env files
.env*

# Large binaries / PDFs
*.pdf

# Local tooling caches / indexes (machine-specific, not source)
.codegraph/
.claudelint-cache/
.remember/
# Impeccable: share config.json, keep the per-machine cache and local overrides out
**/.impeccable/hook.cache.json
**/.impeccable/config.local.json

# Session-generated artifacts
INSIGHTS.md
console.png
